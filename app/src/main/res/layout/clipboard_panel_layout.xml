<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="?attr/keyboardViewBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:padding="4dp"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/back_to_keyboard_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_keyboard"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:tint="?attr/iconColor" />

        <EditText
            android:id="@+id/clipboard_search_edit_text"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:layout_marginEnd="8dp"
            android:hint="Search Clipboard..."
            android:textSize="14sp"
            android:padding="8dp"
            android:background="@drawable/prediction_item_background"
            android:maxLines="1"
            android:inputType="text" />

        <ImageButton
            android:id="@+id/clipboard_backspace_button"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:src="@drawable/ic_backspace"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:tint="?attr/iconColor" />

    </LinearLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/clipboard_recycler_view"
        android:layout_width="match_parent"
        android:layout_height="200dp" />

</LinearLayout>
